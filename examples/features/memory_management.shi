times = (n, f, res) => {
    if (n > 0) {
        times(n-1, f, f(n))
    } else {
        res
    }
}

a = (x, f) => (y) => { f(x, y) }
b = (n) => {
    r = a(1, (x, y) => x + y)
    r(n)
}

times(50000000, b, 0)