sums = (n) => {
    agg = (x, to, sum) => {
        if (x >= to) sum else {
            addition = if (x % 3 == 0 || x % 5 == 0) x else 0
            agg(x + 1, to, sum + addition)
        }
    }
    agg(1, n, 0)
}

sums(1000)